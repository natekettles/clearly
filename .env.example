APPLE_TEAM_ID=
APPLE_ID=
SIGNING_IDENTITY_NAME=

# App Store Connect API (for automated App Review submission)
ASC_KEY_ID=
ASC_ISSUER_ID=
ASC_KEY_FILE=~/.appstoreconnect/AuthKey_XXXXXXXXXX.p8

# Also required for notarization:
# xcrun notarytool store-credentials "AC_PASSWORD" --apple-id "$APPLE_ID" --team-id "$APPLE_TEAM_ID" --password "<app-specific-password>"
